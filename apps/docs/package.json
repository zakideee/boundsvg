{
  "name": "@boundsvg/docs",
  "version": "0.0.0",
  "private": true,
  "description": "Documentation site for boundsvg",
  "license": "MIT OR Apache-2.0",
  "type": "module",
  "scripts": {
    "dev": "test -d public/rustdoc || pnpm -w generate:rustdoc; vitepress dev",
    "build": "vitepress build",
    "preview": "vitepress preview",
    "generate": "tsx scripts/generate-examples.ts"
  },
  "dependencies": {
    "@boundsvg/core": "workspace:*"
  },
  "devDependencies": {
    "vitepress": "^1.6.3"
  }
}
